# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
dist/
build/

# Node / Next.js
node_modules/
.next/
.turbo/
out/
*.tsbuildinfo
.pnpm-store/

# Env / secrets
.env
.env.local
.env.*.local
!.env.example

# Claude Code: machine-specific config (personal allowlists, launch settings)
.claude/settings.local.json
.claude/launch.json

# Data
# Raw PDFs are large — generated by `just scrape`, never committed
data/raw/
data/cache/
data/*.duckdb
data/*.duckdb.wal
# JSON exports for the viewer *are* committed so the web app can build without the pipeline
!data/exports/

# Operational (not part of the submission)
HANDOFF.md

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
